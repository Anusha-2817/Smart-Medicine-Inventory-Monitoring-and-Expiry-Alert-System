// Example file retained for documentation purposes. No runtime dependency on @tanstack/react-start.

export async function getGreeting() {
  return {
    greeting: "Hello!",
    mode: "unknown",
  } as const;
}
